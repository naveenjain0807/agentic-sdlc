---
description: Execute a graph.yaml against url-shortener-service — gates, parallel/sequential dispatch, retries, rollback, re-planning
---

Read `.claude/skills/sdlc-orchestrate/SKILL.md` in full and follow its instructions
exactly. Do not commit or revert anything without asking first, per the skill,
regardless of a node's gate.

Argument (a slug under `orchestrator/runs/`, a path to a `graph.yaml`, or a raw task
description to decompose first): $ARGUMENTS
